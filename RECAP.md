# RECAP - 170tune (proj 63)

updated:  2026-09-22
branch:   main @ c89c990
state:    clean; landed three fixes from one live triage (meatsus, Lab Sus Discord #clankerchat-general, 0x20C2 8GB card), all published to both remotes (LAN + github.com/cachenetics/170tune, main @ cba72b5). (1) STOCK_NDIV catch-22 discoverability: the existing-but-undiscoverable STOCK_NDIV env override is now named inline at the three refusal sites. (2) 170hx-oc reported a driver-dropped VF-offset write as applied (nvml_oc exits 0 even when NVML's readback shows the write did not take - the old `|| echo REFUSED` was dead code); fixed by checking the readback 170hx-oc already computes, not nvml_oc's exit code. (3) THE REAL FIX, prompted by the operator asking to think about 8GB+10GB support: (1) undersold the problem as one card's oddity - it is systematic. cmpunlocker's overclocking/README.md documents THREE real stock points (10GB@45/1215MHz, 8GB-250W@54/1458MHz, 8GB-300W@64/1728MHz, the last being this tool's hardcoded reference), so the catch-22 in (1) was the DEFAULT first-run experience for every 10GB card and every 250W-vbios 8GB card, not an edge case. `known_stock_ndivs()` + an auto-detecting `load_stock_override()` now recognize a card at any of the three documented points with NO override needed; the STOCK_NDIV escape hatch from (1) remains for a genuine fourth/uncatalogued variant. 41 tests green across all three test files (test_170tune.sh 37, test_hbm_test_step.sh 1, test_170hx_oc.sh 3), shellcheck clean on the full CI script list.
next:     none pending - repo is at rest; still owe meatsus the reply confirming the 170hx-oc diagnosis in Discord (Lab Sus #clankerchat-general). Real next step on the code side is qualifying a new operating point on hardware, same as before this triage started.
blocked:  none
settled:  serving default SM offset +250 (not +300/+350, which fault Xid 13 under long soak - quarantined bench-only); HBM serving ceiling NDIV 70 (72 corrupts hot, 74-76 crash, 76 is a field-width eye wall not thermal); REFRESH 24 for serving; 175 W is the sustained-delivery efficiency knee; persistence is receipt-gated per card serial and self-disarms on an unchecked-in boot; unicast FBPA0 PLL unlock (cmpunlocker lineage) is required for the live HBM lever, broadcast-only unlocks are not compatible; "it did not crash" is not a qualification result, only the hot full-VRAM + compute-check gate qualifies a point; the 170HX ships at least THREE factory-stock points, not one - 10GB@NDIV45, 8GB-250W-vbios@NDIV54, 8GB-300W-vbios@NDIV64 (this tool's reference) - the two 8GB variants are not distinguishable from the PCI device id alone, so both stay in the auto-detect set; nvml_oc's exit code proves NVML accepted a write, never that the driver applied it - only readback proves that, and 170hx-oc must check it, not the exit code; a card's power-limit ceiling (e.g. 150 W vs the 300 W reference) is a VBIOS power-management-table value, a different axis from cmpunlocker's driver-level unlock (BAR1/ECC/HBM-PLL) - a low ceiling needs a different VBIOS, not a 170tune fix
docs:     README.md is the entry point; docs/tuning-guide.md (manual, now with a preflight-driver-baked troubleshooting entry in sec 8 reflecting the three-point auto-detect), docs/reference-matrices.md (measured tables), docs/CHANGELOG.md (dated history/retractions) are the living record
tests:    bash tests/test_170tune.sh && bash tests/test_hbm_test_step.sh && bash tests/test_170hx_oc.sh (all three run by .gitlab-ci.yml's `lint` stage); this is a static/build smoke gate only plus now some stubbed CLI-behavior coverage - it never touches hardware, and 170hx-oc still has no coverage beyond what test_170hx_oc.sh added this round. The real correctness proof stays the on-card hot gate (170tune preflight/snapshot-stock/mclk-gate/persist across a reboot)
